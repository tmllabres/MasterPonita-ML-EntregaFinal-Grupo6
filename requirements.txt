# Python 3.12
# Versiones fijadas a propósito: el enunciado exige que el código se ejecute sin
# errores, y "pip install pandas" a secas no garantiza eso dentro de seis meses.
#
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   pip install -r requirements.txt
#
# Congela las versiones REALES cuando termines:  pip freeze > requirements.txt

# ── Datos ────────────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ── Modelos clásicos ─────────────────────────────────────────────────────────
scikit-learn==1.5.2
scipy==1.14.1
joblib==1.4.2

# ── Boosting (elige uno; el enunciado acepta XGBoost, LightGBM o CatBoost) ────
xgboost==2.1.3
# lightgbm==4.5.0

# ── Red neuronal ─────────────────────────────────────────────────────────────
# INSTALA ESTO EL PRIMER DÍA. Es la dependencia que más problemas da en Windows
# y no quieres descubrirlo la víspera de la entrega.
tensorflow==2.18.0

# ── Gráficos ─────────────────────────────────────────────────────────────────
matplotlib==3.9.2
seaborn==0.13.2

# ── Notebooks ────────────────────────────────────────────────────────────────
jupyterlab==4.3.4
ipykernel==6.29.5

# ── Tests ────────────────────────────────────────────────────────────────────
# Los tests de tests/ no son obligatorios, pero el criterio «Calidad del código y
# documentación» pesa un 20 % y un test que falla es la forma más barata de
# demostrar que la limpieza de datos hace lo que dices que hace.
pytest==8.3.4

# ── Bonus (descomenta lo que uses) ───────────────────────────────────────────
# shap==0.46.0
# imbalanced-learn==0.12.4
# mlflow==2.19.0
